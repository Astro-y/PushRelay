HTTP_ADDR=:4426
DATABASE_PATH=./data/pushrelay.db
# 32 random bytes encoded as base64 or 64 hexadecimal characters.
APP_ENCRYPTION_KEY=
# Exact browser origin used for CSRF checks. In production use the public HTTPS domain.
WEB_ORIGIN=http://localhost:5173
TRUSTED_PROXY_CIDRS=
WORKER_CONCURRENCY=8
